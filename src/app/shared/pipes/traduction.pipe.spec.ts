import { TraductionPipe } from './traduction.pipe';

describe('TraductionPipe', () => {
  it('create an instance', () => {
    const pipe = new TraductionPipe();
    expect(pipe).toBeTruthy();
  });
});
